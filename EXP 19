#include <stdio.h>
#include <string.h>

int main() {
    char files[5][20];
    int i, n;

    printf("Enter number of files: ");
    scanf("%d", &n);

    for (i = 0; i < n; i++) {
        printf("Enter file name %d: ", i + 1);
        scanf("%s", files[i]);
    }

    printf("\nFiles in Single Level Directory:\n");
    for (i = 0; i < n; i++)
        printf("%s\n", files[i]);

    return 0;
}
